<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core"
         xmlns:f="/lib/form">
    <f:section title="${%Harbor}">
        <f:entry title="${%Harbor Servers}">
            <f:repeatableHeteroProperty field="servers" hasHeader="true" addCaption="${%AddHarborButton}">
                <div align="right">
                    <f:repeatableDeleteButton/>
                </div>
            </f:repeatableHeteroProperty>
        </f:entry>
    </f:section>
</j:jelly>
